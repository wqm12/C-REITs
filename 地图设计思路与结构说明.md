# 公募 REITs 地图设计思路与结构说明

## 1. 日常更新方式

日常只需要更新根目录下的 `REITs_location_updating.xlsx`，然后运行：

```powershell
python reits_map.py --date 2026-09-03
```

日期参数采用清单命名日。脚本会读取 Excel、`location.csv`、`echarts.min.js`、`china.js` 和根目录的 `map_source_template.py`，并在根目录生成指定日期的：

```text
REITs详细分布图_YYYYMMDD.html
```

过程文件自动进入 `process_files/process_YYYYMMDD_map_generation/`，不会堆在根目录。

## 2. 根目录结构

```text
公募REITs地图项目/
├─ reits_map.py
├─ REITs_location_updating.xlsx
├─ location.csv
├─ echarts.min.js
├─ china.js
├─ REITs详细分布图_YYYYMMDD.html
├─ map_source_template.py
├─ process_files/
├─ 历史版本/
│  ├─ REIT地图历史版本/
│  └─ REIT底层数据历史版本/
└─ 地图设计思路与结构说明.md
```

根目录仅保留正式生成器、生成器依赖、最新结果和本说明文档；历史文件与过程文件分别归档。

## 3. Excel 数据结构

`REITs_location_updating.xlsx` 包含两张表：

- `已上市REITs`
- `未上市REITs`

统一列顺序：

1. 证券代码
2. 证券名称
3. 原始权益人
4. 项目名称
5. REITs大类
6. 资产类型
7. 项目城市
8. 项目省份
9. 项目分类

`REITs大类` 使用 `基础设施REITs` 或 `商业不动产REITs`。每个 REIT 只有一个资产类型；混合业态直接使用例如 `办公+商业零售` 的独立类型。

## 4. 页面结构

- 左上角：全国和重点区域导航，以及已上市/未上市切换。
- 地图：低缩放显示省级分布，高缩放显示城市分布。
- 右侧面板：固定分为 `基础设施REITs` 和 `商业不动产REITs` 两个一级分类。
- 一级分类标题使用与地图一致的蓝灰色标题条，不使用左侧粗竖线。
- 一级分类下：按资产类型、REITs、城市、底层资产逐级展开。
- 搜索：支持按 REITs 名称和底层资产搜索。仅命中一个 REIT 时完整展示其全部城市和底层资产；命中多个 REIT 时，每个结果默认最多展示 3 个城市、每个城市最多 3 项资产。点击“还有几项资产”或“还有几个城市”会完整展开当前 REIT，并同步跳转地图。
- 页面依赖全部内嵌，不依赖外部 CDN。

## 5. 文件管理规则

- 新生成的正式 HTML 保留在根目录。
- 需要保留的旧 HTML、旧脚本放入 `历史版本/REIT地图历史版本/`。
- 旧数据文件放入 `历史版本/REIT底层数据历史版本/`。
- 测试、截图、临时脚本和生成中间文件统一放入 `process_files/`。
- `地图设计思路与结构说明.md` 保留在根目录，便于日常查看。
- 不删除历史文件；需要整理时使用移动归档。
